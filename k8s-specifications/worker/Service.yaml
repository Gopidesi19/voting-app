apiVersion: v1
kind: Service
metadata:
  name: worker-service
  namespace: default
spec:
  type: cluserIP
  selector:
    app: worker-app
  ports:
    - protocol: TCP
    - port: 80
      targetPort: 8082
