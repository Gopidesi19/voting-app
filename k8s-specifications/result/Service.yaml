apiVersion: v1
kind: Service
metadata:
  name: result-service
  namespace: default
spec:
  type: cluserIP
  selector:
    app: result-app
  ports:
    - protocol: TCP
    - port: 80
      targetPort: 8081
