apiVersion: v1
kind: Service
metadata:
  name: redis-service
  namespace: default
spec:
  type: cluserIP
  selector:
    app: redis-app
  ports:
    - protocol: TCP
    - port: 80
      targetPort: 8083
